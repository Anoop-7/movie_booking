CREATE DATABASE movie_booking;
\c movie_booking;
\i D:/node_projects/API/database.sql


delete from users where id=1;
select * from users;

ALTER TABLE movies
DROP COLUMN screen_id;


node

require('crypto').randomBytes(64).toString('hex')


#####################
install nodemon globally
npm install -g nodemon


ALTER TABLE users
ALTER role VARCHAR (100) NOT NULL;

user books ticket:
    user_id
    creation date
    screening_id--> which movie, date, time, theater, screen
    no of tickets

if (req.is_admin)
{
    return res.json("hello admin")
}
return res.json("hello user")

update screens
set capacity = 20
where id = 1;


TODO:
role based screenings data manipulation
cancel bookings


